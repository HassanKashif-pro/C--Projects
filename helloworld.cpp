#include <iostream>
#include <cmath>

int main()
{
    double x, y, z;
    x = 21.32;
    y = 3;

    // z = std::max(x, y)
    // z = std::min(x, y);
    // z = pow(3, 4);
    // z = sqrt(x);
    // z = abs(-4);
    // z = round(x);

    // THer are many more maths functions you an see at (cplusplus.com/maths)

    std::cout << z;
}
